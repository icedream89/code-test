# 当才当用小程序（uni-app）

## 本地预览

```bash
cd app
npm install
npm run dev:h5
```

浏览器预览地址会在终端输出（默认 http://localhost:5173）。

## 微信小程序预览

```bash
cd app
npm install
npm run dev:mp-weixin
```

然后使用微信开发者工具导入生成的 `dist/dev/mp-weixin` 目录。
